# Стек
stack = []
stack.append("А") # добавление элемента(в конец)
stack.append("Б")
print(stack)
up = stack.pop() # удаление элемента(только верхушку)
print(up)
print(stack)

#  Массив
array = [5, 3, 6, 1]
print(array)
# Из массива можно достать определенный элемент по индексу
print(array[2])

array.append(8) # можно добавить элемент
print(array)
arrey.remove(3) # можно удалить элемент
print(array)
